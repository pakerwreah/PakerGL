{
    "tasks": [
        {
            "type": "shell",
            "label": "Build",
            "command": "CXX=clang++ cmake -Bbuild && make -C build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "clear": true
            }
        }
    ],
    "version": "2.0.0"
}